# cc-insights configuration
# Default location: ~/.claude/cc-insights/config.toml
# Override with CCI_CONFIG environment variable.

[proxy]
# Address and port the OTEL proxy listens on
listen = "127.0.0.1:4318"
# Shut down proxy after this duration of inactivity
idle_timeout = "15m"

[upstream]
# Forward OTEL data to this endpoint (leave empty to disable forwarding)
url = ""
# Timeout for upstream requests
timeout = "10s"

# Extra headers to send with forwarded requests
[upstream.headers]
# Authorization = "Bearer YOUR_TOKEN"

[storage]
# Path to the SQLite database file
db_path = "~/.claude/cc-insights/metrics.db"
# Number of days to retain metric data (default: 5 years)
retention_days = 1825

# Model pricing in USD per million tokens.
# Built-in defaults are applied for known models. Only override if pricing changes.

[pricing.opus-4-6]
input = 5.0
output = 25.0
cache_read = 0.50
cache_write = 6.25

[pricing.opus-4-5]
input = 5.0
output = 25.0
cache_read = 0.50
cache_write = 6.25

[pricing.opus-4-1]
input = 15.0
output = 75.0
cache_read = 1.50
cache_write = 18.75

[pricing.sonnet-4-6]
input = 3.0
output = 15.0
cache_read = 0.30
cache_write = 3.75

[pricing.sonnet-4-5]
input = 3.0
output = 15.0
cache_read = 0.30
cache_write = 3.75

[pricing.sonnet-4-0]
input = 3.0
output = 15.0
cache_read = 0.30
cache_write = 3.75

[pricing.haiku-4-5]
input = 1.0
output = 5.0
cache_read = 0.10
cache_write = 1.25

[pricing.haiku-3-5]
input = 0.80
output = 4.0
cache_read = 0.08
cache_write = 1.0
